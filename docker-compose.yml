version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: spaghetti-backend
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3000
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - CORS_ORIGINS=${CORS_ORIGINS:-chrome-extension://*,http://localhost:*}
      - RATE_LIMIT_WINDOW_MS=${RATE_LIMIT_WINDOW_MS:-900000}
      - RATE_LIMIT_MAX_REQUESTS=${RATE_LIMIT_MAX_REQUESTS:-100}
      - ENABLE_HELMET=${ENABLE_HELMET:-true}
      - TRUST_PROXY=${TRUST_PROXY:-true}
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/health', (r) => process.exit(r.statusCode === 200 ? 0 : 1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - spaghetti-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Future: Add database service
  # postgres:
  #   image: postgres:16-alpine
  #   container_name: spaghetti-db
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: ${DB_USER:-spaghetti}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD}
  #     POSTGRES_DB: ${DB_NAME:-spaghetti}
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   networks:
  #     - spaghetti-network

  # Future: Add Redis for caching
  # redis:
  #   image: redis:7-alpine
  #   container_name: spaghetti-cache
  #   restart: unless-stopped
  #   networks:
  #     - spaghetti-network

networks:
  spaghetti-network:
    driver: bridge

# volumes:
#   postgres_data:
